# mel Feature

## Purpose
This module handles all frontend logic related to **mel**.

## Structure
- api/ ? API calls to backend endpoints
- components/ ? UI components
- hooks/ ? React hooks for state/data logic
- types/ ? TypeScript definitions

## Backend Mapping
Connects to backend /api/mel/ endpoints (or related domain APIs).
