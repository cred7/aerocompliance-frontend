# amp Feature

## Purpose
This module handles all frontend logic related to **amp**.

## Structure
- api/ ? API calls to backend endpoints
- components/ ? UI components
- hooks/ ? React hooks for state/data logic
- types/ ? TypeScript definitions

## Backend Mapping
Connects to backend /api/amp/ endpoints (or related domain APIs).
